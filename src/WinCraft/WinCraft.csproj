<Project Sdk="Microsoft.NET.Sdk.WindowsDesktop">
  <Import Project="..\..\publish\version.props" />
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFrameworks>net30;net45</TargetFrameworks>
    <UseWPF>true</UseWPF>
    <EnableDefaultApplicationDefinition>false</EnableDefaultApplicationDefinition>
    <ApplicationIcon>..\..\assets\app.ico</ApplicationIcon>
    <ApplicationManifest>Properties\app.manifest</ApplicationManifest>
    <AppConfig>Properties\app.config</AppConfig>
    <StartupObject>WinCraft.Program</StartupObject>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\Directory.Build.props" Link="Properties\Directory.Build.props" />
    <None Include="..\..\publish\version.props" Link="Properties\version.props" />
    <None Include="..\..\assets\app.ico" Link="Properties\app.ico" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\WinCraft.Core\WinCraft.Core.csproj" />
    <ProjectReference Include="..\WinCraft.Lzma\WinCraft.Lzma.csproj"
                      ReferenceOutputAssembly="false"
                      OutputItemType="Content"
                      CopyToOutputDirectory="PreserveNewest"
                      PrivateAssets="all" />
  </ItemGroup>

  <!-- The overlay needs WinCraft.Lzma.dll in the shared output directory, but
       the DLL remains owned by the WinCraft.Lzma project for clean/rebuild. -->
  <Target Name="ExcludeLzmaProjectOutputFromClean"
          AfterTargets="_CleanGetCurrentAndPriorFileWrites">
    <ItemGroup>
      <_WinCraftLzmaProjectOutput Include="$(OutDir)WinCraft.Lzma.dll" />
      <_WinCraftLzmaProjectOutput Include="$(OutDir)WinCraft.Lzma.pdb" />
      <_CleanPriorFileWrites Remove="@(_WinCraftLzmaProjectOutput)" />
      <_CleanCurrentFileWrites Remove="@(_WinCraftLzmaProjectOutput)" />
    </ItemGroup>
  </Target>

  <ItemGroup>
    <Reference Include="Microsoft.VisualBasic" />
  </ItemGroup>


</Project>
