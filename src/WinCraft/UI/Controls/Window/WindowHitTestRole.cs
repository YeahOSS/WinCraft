namespace WinCraft.UI
{
    /// <summary>Non-client behavior assigned to a title-bar element.</summary>
    public enum WindowHitTestRole
    {
        None,
        Client,
        Caption,
        Minimize,
        Maximize,
        Close,
        Help,
    }
}
